<UserControl x:Class="AdminPanelPro.Views.EntitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Entities}"
                  SelectedItem="{Binding SelectedEntity}"
                  AutoGenerateColumns="False">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Название"
                                    Binding="{Binding Name}" />

                <DataGridTemplateColumn Header="Тип">

                    <!-- отображение -->
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ScorableEntityType}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>

                    <!-- редактирование -->
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox
                                ItemsSource="{Binding DataContext.EntityTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                SelectedItem="{Binding ScorableEntityType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>

                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button Content="Создать"
                    Command="{Binding CreateCommand}"
                    Width="100"
                    Margin="5"/>

            <Button Content="Обновить"
                    Command="{Binding UpdateCommand}"
                    Width="100"
                    Margin="5"/>

            <Button Content="Удалить"
                    Command="{Binding DeleteCommand}"
                    Width="100"
                    Margin="5"/>

        </StackPanel>

    </Grid>

</UserControl>